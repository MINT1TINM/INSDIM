extends layout

block content
  section
    h1= __("news")
    
    each item in newsList
      div(class="news-item")
        div(class="news-title")= item.title
        p!= item.content
        a(href="/news/"+item._id class="news-link")
          span learn more

      hr